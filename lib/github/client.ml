(* GitHub REST API client — placeholder *)

type github_config = {
  token : string;
  owner : string;
  repo : string;
  base_url : string;
}

let _placeholder = { token = ""; owner = ""; repo = ""; base_url = "https://api.github.com" }
